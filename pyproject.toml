[project]
name = "admixture-identifiability"
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
version = "0.1.0"

dependencies = [
  "numpy>=1.20,<2.0",       # add a conservative upper bound
  "scipy>=1.7,<2.0",
  "matplotlib>=3.5",
  "torch>=2.0",
  "tqdm>=4.0"
]